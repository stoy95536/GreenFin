# GreenFin Backend Dependencies
#
# Storage is JSON file-based per ADR-0006, so there is no SQL driver or migration
# tool here. SQLAlchemy/Alembic were removed in the 2026-08-17 architecture cleanup
# because they had no runtime consumers (see ADR-0007).

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Validation & settings
pydantic==2.10.4
pydantic-settings==2.7.1

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.4
httpx==0.28.1

# ASGI
anyio==4.8.0
